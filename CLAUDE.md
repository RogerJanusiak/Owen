# Project Instructions

- Write the most human-readable code possible: clear names, simple structure, no clever tricks.
- Run `npm test` after every code change and confirm all tests pass before reporting done.
- Never modify files in `test/` unless the user explicitly asks.
